#!/usr/bin/make -f
export PYBUILD_NAME=opentelemetry-util-http
export PYBUILD_DIR=util/opentelemetry-util-http

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_test:
	# Skip tests - test dependencies not packaged
